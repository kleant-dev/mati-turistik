export type ExperienceItem = {
  image: string;
  label: string;
  description: string;
};

export type DestinationItem = {
  id: number;
  title: string;
  location: string;
  src: string;
};

export type TestimonialItem = {
  name: string;
  testimonial: string;
  image: string;
};
